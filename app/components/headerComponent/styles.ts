import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  header: {
    justifyContent: "center",
    alignItems: "center",
    flexDirection: "row",
    backgroundColor: "#3B63F1",
  },
  title: {
    margin: 20,
    fontSize: 30,
    color: "white",
  },
});

export default styles;
